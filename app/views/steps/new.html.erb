<h2>New Step</h2>

<%= form_for [@task, @step] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<%= f.submit :class => "btn btn-primary" %>
<% end %>
