<h2><%= @task.name %></h2>
<ol>
	<% @task.steps.each do |step| %>
	<li><%= link_to step.name, task_step_path(@task) %></li>
	<% end %>
</ol>
<p><%= link_to "Add a Step", new_task_step_path(@task) %></p>
<p><%= link_to "All Tasks", tasks_path %></p>
